import styled from 'styled-components';

export const ScrollContainer = styled.div`
  width: 100%;

  display: flex;
  flex-wrap: nowrap;
  overflow-x: auto;

  padding-bottom: 10px;
  gap: 10px;
  margin: 0 auto;
`;
